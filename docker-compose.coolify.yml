version: '3.8'

services:
  ui:
    build:
      context: ./ui
      dockerfile: Dockerfile
    depends_on:
      - api
    networks:
      - app-network

  api:
    build:
      context: ./api
      dockerfile: Dockerfile
    networks:
      - app-network

networks:
  app-network:
    driver: bridge
